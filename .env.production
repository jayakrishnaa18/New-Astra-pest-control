# Production Environment Variables for Render
# Copy these to Render Dashboard -> Environment

NODE_ENV=production
PORT=10000

# SMTP Settings - IMPORTANT: Render blocks Gmail SMTP!
# Option 1: Use SendGrid (Free tier: 100 emails/day)
#   1. Sign up at https://sendgrid.com
#   2. Create API Key at Settings -> API Keys
#   3. Use these settings:
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=your-sendgrid-api-key-here

# Option 2: Use Gmail (Only works locally, NOT on Render free tier)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-gmail-address
# SMTP_PASS=your-gmail-app-password

# Email Recipients
EMAIL_TO=your-email@domain.com

# Client URL (Update with your actual Render URL)
CLIENT_URL=https://astra-pest-control-v1.onrender.com
